title = 'nombre ventana cmd'; 
central_url= 'url de la central';
puerto_controladores = 9000; // Normalmente después del 9000
puerto_api = 8000; // Normalmente después del 3000

module.exports = {
    title,
    central_url,
    puerto_controladores,
    puerto_api
}

